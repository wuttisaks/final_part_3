<%= form_for(@kingsong2) do |f| %>
  <% if @kingsong2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kingsong2.errors.count, "error") %> prohibited this kingsong2 from being saved:</h2>

      <ul>
      <% @kingsong2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
